import 'package:flutter/material.dart';

class PoseCameraPage extends StatelessWidget {
  const PoseCameraPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Kamera Analiz'),
      ),
      body: const Center(
        child: Text('Pose Camera Page (Planlanan UI)'),
      ),
    );
  }
}
